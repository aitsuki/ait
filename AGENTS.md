# Agent 规则

- `superpowers` 提供的规格文档必须为中文，计划文档不做要求。
- `superpowers` 在提供计划文档后禁止自动执行。
- `superpowers` 在执行计划文档后，需要勾选已完成的步骤。
- 禁止使用 `superpowers:subagent-driven-development`
- 禁止使用 `superpowers:using-git-worktrees`